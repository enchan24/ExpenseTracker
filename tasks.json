{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "run c",
            "type": "shell",
            "command": "gcc main.c -o main.exe && main.exe",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}